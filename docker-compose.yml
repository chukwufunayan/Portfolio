services:
  portfolio:
    container_name: portfolio_develop
    image: node:18.3.0-alpine3.16
    ports:
      - 3000:3000
    working_dir: /app
    volumes:
      - ./:/app
      - /app/node_modules
    command: ["sh","-c","npm install && npm start"]

